-- ================================================
-- STEAL AN EGG HUB — FULL BUILD v3 (BYPASS HARDENED)
-- 7 onglets | Rayfield Library
-- ================================================

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Players         = game:GetService("Players")
local RunService      = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService    = game:GetService("TweenService")
local PhysicsService  = game:GetService("PhysicsService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local Mouse       = LocalPlayer:GetMouse()

-- ================================================
-- ÉTAT GLOBAL
-- ================================================

local State = {
    -- Mouvement
    WalkSpeed    = 16,
    InfJump      = false,
    NoClip       = false,
    SpeedBoost   = false,
    -- Egg
    AutoFarm     = false,
    EggType      = "All",
    AutoSell     = false,
    AutoHatch    = false,
    -- Combat
    AutoClick    = false,
    AutoClickCPS = 20,
    AimbotOn     = false,
    AimbotRange  = 50,
    -- ESP
    ESPPlayers   = false,
    ESPEggs      = false,
    ESPItems     = false,
    -- Misc
    AntiAFK      = false,
    NoFog        = false,
    InfCoins     = false,
    -- Server
    ServerRegion = "Auto",
}

local Connections    = {}
local ESPFolder      = nil
local AutoFarmThread = nil
local AutoSellThread = nil
local AimbotThread   = nil
local AntiAFKConn    = nil

-- ================================================
-- UTILITAIRES & BYPASS DE FIRE ROBUSTE
-- ================================================

local function GetCharacter()
    return LocalPlayer.Character
end

local function GetHumanoid()
    local c = GetCharacter()
    return c and c:FindFirstChildOfClass("Humanoid")
end

local function GetHRP()
    local c = GetCharacter()
    return c and c:FindFirstChild("HumanoidRootPart")
end

local function Notify(title, msg, duration)
    Rayfield:Notify({ Title = title, Content = msg, Duration = duration or 3, Image = 4483362458 })
end

local function ClearConnections()
    for _, c in ipairs(Connections) do
        if c then c:Disconnect() end
    end
    Connections = {}
end

-- Bypass universel pour déclencher les interactions (Anti-Anti-Cheat / Remote / Proximity)
local function BypassFire(instance)
    if not instance then return end
    
    -- 1. ClickDetector natif ou custom
    local cd = instance:FindFirstChildOfClass("ClickDetector") or (instance:IsA("Model") and instance:FindFirstChild("ClickDetector", true))
    if cd then
        pcall(function()
            fireclickdetector(cd)
        end)
    end

    -- 2. ProximityPrompt avec modification instantanée de la distance/durée si requis
    local pp = instance:FindFirstChildOfClass("ProximityPrompt") or (instance:IsA("Model") and instance:FindFirstChild("ProximityPrompt", true))
    if pp then
        pcall(function()
            pp.HoldDuration = 0
            pp.MaxActivationDistance = math.huge
            fireproximityprompt(pp)
        end)
    end

    -- 3. TouchInterest (pour les objets ramassables par contact physique)
    local touchPart = instance:IsA("BasePart") and instance or (instance:IsA("Model") and (instance.PrimaryPart or instance:FindFirstChildOfClass("BasePart")))
    local hrp = GetHRP()
    if touchPart and hrp then
        pcall(function()
            local touch = touchPart:FindFirstChildOfClass("TouchTransmitter") or touchPart:FindFirstChild("TouchInterest")
            if touch then
                firetouchinterest(hrp, touchPart, 0)
                task.wait()
                firetouchinterest(hrp, touchPart, 1)
            end
        end)
    end
end

-- Recherche élargie insensible à la casse
local EGG_KEYWORDS  = {"egg","oeuf","coin","reward","drop"}
local SELL_KEYWORDS = {"sell","shop","vendor","npc","bank","exchange"}
local ITEM_KEYWORDS = {"collectible","pickup","item","drop","chest"}

local function MatchKeywords(name, keywords)
    local lowerName = string.lower(name)
    for _, kw in ipairs(keywords) do
        if string.find(lowerName, string.lower(kw), 1, true) then return true end
    end
    return false
end

local function GetEggsInWorkspace()
    local results = {}
    for _, obj in ipairs(workspace:GetDescendants()) do
        if (obj:IsA("BasePart") or obj:IsA("Model")) and MatchKeywords(obj.Name, EGG_KEYWORDS) then
            if State.EggType == "All" or string.find(string.lower(obj.Name), string.lower(State.EggType), 1, true) then
                table.insert(results, obj)
            end
        end
    end
    return results
end

local function GetSellPoints()
    local results = {}
    for _, obj in ipairs(workspace:GetDescendants()) do
        if (obj:IsA("BasePart") or obj:IsA("Model")) and MatchKeywords(obj.Name, SELL_KEYWORDS) then
            table.insert(results, obj)
        end
    end
    return results
end

local function GetPivot(obj)
    if obj:IsA("Model") then
        local ok, cf = pcall(function() return obj:GetPivot() end)
        if ok then return cf end
        local p = obj.PrimaryPart or obj:FindFirstChildOfClass("BasePart")
        return p and p.CFrame or nil
    elseif obj:IsA("BasePart") then
        return obj.CFrame
    end
    return nil
end

local function TeleportTo(cf)
    local hrp = GetHRP()
    if hrp and cf then
        hrp.CFrame = cf + Vector3.new(0, 3, 0)
    end
end

-- ================================================
-- MOUVEMENT
-- ================================================

local function ApplyWalkSpeed(v)
    local h = GetHumanoid()
    if h then h.WalkSpeed = v end
end

local function StartInfJump()
    local conn = UserInputService.JumpRequest:Connect(function()
        if not State.InfJump then return end
        local h = GetHumanoid()
        if h then h:ChangeState(Enum.HumanoidStateType.Jumping) end
    end)
    table.insert(Connections, conn)
end

local NoClipConn = nil
local function StartNoClip()
    NoClipConn = RunService.Stepped:Connect(function()
        if not State.NoClip then return end
        local c = GetCharacter()
        if not c then return end
        for _, p in ipairs(c:GetDescendants()) do
            if p:IsA("BasePart") then
                p.CanCollide = false
            end
        end
    end)
end

-- ================================================
-- EGG — FARM AUTOMATIQUE OPTIMISÉ
-- ================================================

local function StopAutoFarm()
    State.AutoFarm = false
    AutoFarmThread = nil
end

local function StartAutoFarm()
    if AutoFarmThread then return end
    AutoFarmThread = task.spawn(function()
        while State.AutoFarm do
            local eggs = GetEggsInWorkspace()
            for _, egg in ipairs(eggs) do
                if not State.AutoFarm then break end
                local cf = GetPivot(egg)
                if cf then
                    TeleportTo(cf)
                    task.wait(0.08)
                    BypassFire(egg)
                    task.wait(0.05)
                end
            end
            task.wait(0.2)
        end
        AutoFarmThread = nil
    end)
end

-- ================================================
-- EGG — AUTO VENTE
-- ================================================

local function StopAutoSell()
    State.AutoSell = false
    AutoSellThread = nil
end

local function StartAutoSell()
    if AutoSellThread then return end
    AutoSellThread = task.spawn(function()
        while State.AutoSell do
            local points = GetSellPoints()
            for _, sp in ipairs(points) do
                if not State.AutoSell then break end
                local cf = GetPivot(sp)
                if cf then
                    TeleportTo(cf)
                    task.wait(0.1)
                    BypassFire(sp)
                end
            end
            task.wait(1.5)
        end
        AutoSellThread = nil
    end)
end

-- ================================================
-- EGG — AUTO ÉCLUSION (HATCH)
-- ================================================

local AutoHatchThread = nil
local function StartAutoHatch()
    if AutoHatchThread then return end
    AutoHatchThread = task.spawn(function()
        while State.AutoHatch do
            for _, obj in ipairs(workspace:GetDescendants()) do
                if not State.AutoHatch then break end
                if (obj:IsA("BasePart") or obj:IsA("Model")) and MatchKeywords(obj.Name, {"hatch","open","incubator"}) then
                    local cf = GetPivot(obj)
                    if cf then
                        TeleportTo(cf)
                        task.wait(0.1)
                        BypassFire(obj)
                    end
                end
            end
            task.wait(0.5)
        end
        AutoHatchThread = nil
    end)
end

-- ================================================
-- COMBAT — AUTO-CLICK
-- ================================================

local AutoClickThread = nil
local function StartAutoClick()
    if AutoClickThread then return end
    AutoClickThread = task.spawn(function()
        while State.AutoClick do
            local target = Mouse.Target
            if target then
                BypassFire(target)
            end
            task.wait(1 / State.AutoClickCPS)
        end
        AutoClickThread = nil
    end)
end

-- ================================================
-- COMBAT — AIMBOT
-- ================================================

local function GetNearestPlayer()
    local hrp = GetHRP()
    if not hrp then return nil end
    local best, bestDist = nil, State.AimbotRange

    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local th = p.Character:FindFirstChild("HumanoidRootPart")
            local hm = p.Character:FindFirstChildOfClass("Humanoid")
            if th and hm and hm.Health > 0 then
                local dist = (hrp.Position - th.Position).Magnitude
                if dist < bestDist then
                    bestDist = dist
                    best = th
                end
            end
        end
    end
    return best
end

local function StartAimbot()
    if AimbotThread then return end
    AimbotThread = task.spawn(function()
        while State.AimbotOn do
            local target = GetNearestPlayer()
            if target then
                local camera = workspace.CurrentCamera
                camera.CFrame = CFrame.new(camera.CFrame.Position, target.Position)
            end
            task.wait(0.016)
        end
        AimbotThread = nil
    end)
end

-- ================================================
-- ESP — SYSTÈME DE SURBRILLANCE
-- ================================================

local function ClearESP()
    if ESPFolder then
        ESPFolder:Destroy()
        ESPFolder = nil
    end
end

local function MakeHighlight(adornee, fillColor, outlineColor)
    if not ESPFolder then return end
    for _, h in ipairs(ESPFolder:GetChildren()) do
        if h:IsA("Highlight") and h.Adornee == adornee then return end
    end
    local h = Instance.new("Highlight")
    h.Adornee             = adornee
    h.FillColor           = fillColor
    h.OutlineColor        = outlineColor
    h.FillTransparency    = 0.5
    h.OutlineTransparency = 0
    h.DepthMode           = Enum.HighlightDepthMode.AlwaysOnTop
    h.Parent              = ESPFolder
end

local function BuildESP()
    ClearESP()
    ESPFolder = Instance.new("Folder")
    ESPFolder.Name   = "AXM_ESP"
    ESPFolder.Parent = workspace

    if State.ESPPlayers then
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                MakeHighlight(p.Character, Color3.fromRGB(255, 80, 80), Color3.fromRGB(255, 255, 255))
            end
        end
        local c1 = Players.PlayerAdded:Connect(function(p)
            p.CharacterAdded:Connect(function(char)
                task.wait(0.5)
                if State.ESPPlayers and ESPFolder then
                    MakeHighlight(char, Color3.fromRGB(255, 80, 80), Color3.fromRGB(255, 255, 255))
                end
            end)
        end)
        table.insert(Connections, c1)
    end

    if State.ESPEggs then
        for _, obj in ipairs(GetEggsInWorkspace()) do
            MakeHighlight(obj, Color3.fromRGB(255, 220, 50), Color3.fromRGB(255, 255, 255))
        end
        local c2 = workspace.DescendantAdded:Connect(function(obj)
            if not State.ESPEggs or not ESPFolder then return end
            if (obj:IsA("BasePart") or obj:IsA("Model")) and MatchKeywords(obj.Name, EGG_KEYWORDS) then
                task.wait(0.1)
                MakeHighlight(obj, Color3.fromRGB(255, 220, 50), Color3.fromRGB(255, 255, 255))
            end
        end)
        table.insert(Connections, c2)
    end

    if State.ESPItems then
        for _, obj in ipairs(workspace:GetDescendants()) do
            if (obj:IsA("BasePart") or obj:IsA("Model")) and MatchKeywords(obj.Name, ITEM_KEYWORDS) then
                MakeHighlight(obj, Color3.fromRGB(80, 200, 255), Color3.fromRGB(255, 255, 255))
            end
        end
    end
end

local function RefreshESP()
    ClearConnections()
    if State.ESPPlayers or State.ESPEggs or State.ESPItems then
        BuildESP()
    else
        ClearESP()
    end
end

-- ================================================
-- MISC — ANTI-AFK
-- ================================================

local function StartAntiAFK()
    AntiAFKConn = RunService.Heartbeat:Connect(function()
        if not State.AntiAFK then return end
        local cam = workspace.CurrentCamera
        if cam then
            cam.CFrame = cam.CFrame * CFrame.Angles(0, math.rad(0.001), 0)
        end
    end)
end

-- ================================================
-- MISC — NO FOG (SANS BROUILLARD)
-- ================================================

local function ApplyNoFog(enabled)
    local lighting = game:GetService("Lighting")
    if enabled then
        for _, effect in ipairs(lighting:GetChildren()) do
            if effect:IsA("FogEffect") or effect:IsA("Atmosphere") then
                effect.Enabled = false
            end
        end
        lighting.FogEnd   = 100000
        lighting.FogStart = 99999
    else
        lighting.FogEnd   = 100000
        lighting.FogStart = 0
    end
end

-- ================================================
-- RESPAWN
-- ================================================

LocalPlayer.CharacterAdded:Connect(function(character)
    task.wait(0.6)
    local hum = character:WaitForChild("Humanoid", 5)
    if hum then
        hum.WalkSpeed = State.WalkSpeed
    end
    if State.InfJump then StartInfJump() end
    if State.NoClip then StartNoClip() end
    if State.ESPPlayers or State.ESPEggs or State.ESPItems then
        task.wait(0.3)
        BuildESP()
    end
    Notify("Respawn détecté", "Paramètres réappliqués.", 2)
end)

-- ================================================
-- INTERFACE RAYFIELD UI
-- ================================================

local Window = Rayfield:CreateWindow({
    Name = "Steal an Egg Hub [Bypass v3]",
    LoadingTitle = "Chargement...",
    LoadingSubtitle = "Sécurité & Bypass intégrés",
    Theme = "Default",
    ConfigurationSaving = { Enabled = true, FolderName = "AXM_StealEgg", FileName = "config_v3" },
    KeySystem = false,
})

-- Onglet 1 — Mouvement
local TabMove = Window:CreateTab("Mouvement", 4483362458)
TabMove:CreateSlider({
    Name = "Vitesse de marche (WalkSpeed)", Range = {16, 150}, Increment = 1, Suffix = " studs/s", CurrentValue = 16, Flag = "WalkSpeed",
    Callback = function(v) State.WalkSpeed = v ApplyWalkSpeed(v) end,
})
TabMove:CreateToggle({
    Name = "Saut Infini (Inf Jump)", CurrentValue = false, Flag = "InfJump",
    Callback = function(v) State.InfJump = v if v then StartInfJump() end end,
})
TabMove:CreateToggle({
    Name = "Traverser les murs (No Clip)", CurrentValue = false, Flag = "NoClip",
    Callback = function(v) State.NoClip = v if v then StartNoClip() else if NoClipConn then NoClipConn:Disconnect() NoClipConn = nil end end end,
})

-- Onglet 2 — Œufs (Farm avec Bypass)
local TabEgg = Window:CreateTab("Œufs", 4483362458)
TabEgg:CreateDropdown({
    Name = "Type d'œuf", Options = {"All", "Common", "Uncommon", "Rare", "Epic", "Legendary", "Golden", "Mythical"},
    CurrentOption = {"All"}, MultipleOptions = false, Flag = "EggType",
    Callback = function(selected) State.EggType = selected[1] or "All" end,
})
TabEgg:CreateToggle({
    Name = "Auto Farm d'œufs (Bypassé)", CurrentValue = false, Flag = "AutoFarm",
    Callback = function(v) State.AutoFarm = v if v then StartAutoFarm() else StopAutoFarm() end end,
})
TabEgg:CreateToggle({
    Name = "Auto Vente", CurrentValue = false, Flag = "AutoSell",
    Callback = function(v) State.AutoSell = v if v then StartAutoSell() else StopAutoSell() end end,
})
TabEgg:CreateToggle({
    Name = "Auto Éclosion (Hatch)", CurrentValue = false, Flag = "AutoHatch",
    Callback = function(v) State.AutoHatch = v if v then StartAutoHatch() else State.AutoHatch = false end end,
})

-- Onglet 3 — Boutique / Vente
local TabShop = Window:CreateTab("Boutique", 4483362458)
TabShop:CreateButton({
    Name = "Se téléporter au point de vente le plus proche",
    Callback = function()
        local pts = GetSellPoints()
        if #pts > 0 then TeleportTo(GetPivot(pts[1])) end
    end,
})

-- Onglet 4 — ESP
local TabESP = Window:CreateTab("ESP", 4483362458)
TabESP:CreateToggle({ Name = "ESP Joueurs", CurrentValue = false, Flag = "ESPPlayers", Callback = function(v) State.ESPPlayers = v RefreshESP() end })
TabESP:CreateToggle({ Name = "ESP Œufs", CurrentValue = false, Flag = "ESPEggs", Callback = function(v) State.ESPEggs = v RefreshESP() end })
TabESP:CreateToggle({ Name = "ESP Objets", CurrentValue = false, Flag = "ESPItems", Callback = function(v) State.ESPItems = v RefreshESP() end })

-- Onglet 5 — Divers (Misc)
local TabMisc = Window:CreateTab("Divers", 4483362458)
TabMisc:CreateToggle({ Name = "Anti-AFK", CurrentValue = false, Flag = "AntiAFK", Callback = function(v) State.AntiAFK = v if v then StartAntiAFK() end end })
TabMisc:CreateToggle({ Name = "Sans Brouillard (No Fog)", CurrentValue = false, Flag = "NoFog", Callback = function(v) ApplyNoFog(v) end })

-- Onglet 6 — Combat
local TabCombat = Window:CreateTab("Combat", 4483362458)
TabCombat:CreateToggle({ Name = "Auto-Click", CurrentValue = false, Flag = "AutoClick", Callback = function(v) State.AutoClick = v if v then StartAutoClick() end end })
TabCombat:CreateToggle({ Name = "Aimbot", CurrentValue = false, Flag = "AimbotOn", Callback = function(v) State.AimbotOn = v if v then StartAimbot() end end })

-- Onglet 7 — Serveur
local TabServer = Window:CreateTab("Serveur", 4483362458)
TabServer:CreateButton({ Name = "Copier le Job ID", Callback = function() print(game.JobId) end })

Notify("Steal an Egg Hub", "Mise à jour v3 (Bypass intégré) prête.", 4)
